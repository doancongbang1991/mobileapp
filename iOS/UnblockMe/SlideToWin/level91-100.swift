//
//  level91-100.swift
//  SlideToWin
//
//  Created by djay mac on 07/01/15.
//  Copyright (c) 2015 DJay. All rights reserved.
//

import UIKit




func runlevel91() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock1                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock3, yblock1)                // vertical block small
    (vbs1x, vbs1y)  = (xblock3, yblock3)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock4, yblock1)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock6, yblock2)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock6, yblock5)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock1, yblock4, blocksize3)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock, yblock, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock, yblock, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock1)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock1, yblock2)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock2, yblock5)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock2, yblock6)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock4, yblock6)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock5, yblock1, blocksize2)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock5, yblock4, blocksize2)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}



func runlevel92() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock3                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock2, yblock2)                // vertical block small
    (vbs1x, vbs1y)  = (xblock3, yblock1)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock3, yblock4)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock4, yblock1)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock5, yblock3)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock6, yblock2, blocksize2)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock1, yblock1, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock6, yblock4, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock4)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock1, yblock6)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock4, yblock5)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock5, yblock1)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock3, yblock6, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock, yblock, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}


func runlevel93() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock1                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock1, yblock1)                // vertical block small
    (vbs1x, vbs1y)  = (xblock3, yblock3)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock4, yblock3)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock4, yblock5)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock6, yblock3)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock5, yblock2, blocksize3)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock, yblock, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock, yblock, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock4)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock2, yblock6)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock5, yblock5)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock, yblock)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock2, yblock1, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock2, yblock2, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}


func runlevel94() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock1                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock1, yblock4)                // vertical block small
    (vbs1x, vbs1y)  = (xblock3, yblock5)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock5, yblock1)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock5, yblock3)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock, yblock)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock4, yblock1, blocksize3)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock6, yblock3, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock, yblock, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock6)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock3, yblock4)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock4, yblock5)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock, yblock)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock1, yblock1, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock1, yblock2, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}



func runlevel95() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock2                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock1, yblock1)                // vertical block small
    (vbs1x, vbs1y)  = (xblock2, yblock4)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock3, yblock1)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock4, yblock2)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock6, yblock3)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock6, yblock5, blocksize2)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock1, yblock3, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock5, yblock4, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock6)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock3, yblock5)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock3, yblock6)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock5, yblock2)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock4, yblock1, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock, yblock, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}





func runlevel96() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock4                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock2, yblock5)                // vertical block small
    (vbs1x, vbs1y)  = (xblock3, yblock2)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock3, yblock4)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock4, yblock4)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock, yblock)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock6, yblock1, blocksize3)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock, yblock, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock, yblock, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock4)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock5, yblock5)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock, yblock)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock, yblock)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock1, yblock1, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock3, yblock6, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}





func runlevel97() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock2                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock3, yblock5)                // vertical block small
    (vbs1x, vbs1y)  = (xblock4, yblock2)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock4, yblock4)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock, yblock)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock, yblock)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock1, yblock1, blocksize3)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock5, yblock2, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock6, yblock2, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock4, yblock6)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock, yblock)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock, yblock)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock, yblock)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock1, yblock4, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock3, yblock1, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}



func runlevel98() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock1                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock1, yblock4)                // vertical block small
    (vbs1x, vbs1y)  = (xblock3, yblock5)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock4, yblock1)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock4, yblock3)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock5, yblock1)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock5, yblock3, blocksize2)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock6, yblock1, blocksize2)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock6, yblock3, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock2)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock1, yblock6)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock2, yblock4)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock4, yblock5)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock1, yblock1, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock, yblock, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}


func runlevel99() {
    
    mainx = xblock1                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock1, yblock4)                // vertical block small
    (vbs1x, vbs1y)  = (xblock3, yblock5)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock5, yblock1)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock5, yblock3)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock6, yblock2)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock6, yblock4, blocksize2)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock4, yblock1, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock, yblock, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock6)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock3, yblock4)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock4, yblock5)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock, yblock)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock, yblock, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock, yblock, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}



func runlevel100() {
    
       // set the values depending on level(to hide set the value to "no")
    mainx = xblock1                                     // main block only x can be changed
    (vbsx, vbsy)    = (xblock1, yblock1)                // vertical block small
    (vbs1x, vbs1y)  = (xblock3, yblock5)                // vertical block small 1
    (vbs2x, vbs2y)  = (xblock4, yblock2)                // vertical block small 2
    (vbs3x, vbs3y)  = (xblock4, yblock4)                // vertical block small 3
    (vbs4x, vbs4y)  = (xblock5, yblock3)                // vertical block small 4
    
    (vbbx, vbby, sizevbb)    = (xblock3, yblock1, blocksize2)                          // vertical block big
    (vbb1x, vbb1y, sizevbb1)  = (xblock6, yblock2, blocksize3)                          // vertical block big  1
    (vbb2x, vbb2y, sizevbb2)  = (xblock, yblock, blocksize3)                          // vertical block big  2
    (vbb3x, vbb3y, sizevbb3)  = (xblock, yblock, blocksize3)                          // vertical block big  3
    (vbb4x, vbb4y, sizevbb4)  = (xblock, yblock, blocksize3)                          // vertical block big 4
    
    (hbsx, hbsy)    = (xblock1, yblock6)                          // horizontal block small
    (hbs1x, hbs1y)  = (xblock4, yblock6)                          // horizontal block small 1
    (hbs2x, hbs2y)  = (xblock5, yblock5)                          // horizontal block small 2
    (hbs3x, hbs3y)  = (xblock, yblock)                          // horizontal block small 3
    (hbs4x, hbs4y)  = (xblock, yblock)                          // horizontal block small 4
    
    (hbbx, hbby, sizehbb)    = (xblock1, yblock4, blocksize3)                          // horizontal block big
    (hbb1x, hbb1y, sizehbb1)  = (xblock4, yblock1, blocksize3)                          // horizontal block big 1
    (hbb2x, hbb2y, sizehbb2)  = (xblock, yblock, blocksize3)                          // horizontal block big 2
    (hbb3x, hbb3y, sizehbb3)  = (xblock, yblock, blocksize3)                          // horizontal block big 3
    (hbb4x, hbb4y, sizehbb4)  = (xblock, yblock, blocksize3)                          // horizontal block big 4
    

    
}